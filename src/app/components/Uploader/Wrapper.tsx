import { Upload } from 'antd';
import styled from 'styled-components';

type Props = {};
export const UploadWrapper = styled(Upload)<Props>`
  .ant-upload {
    width: 128px;
    height: 128px;
  }
`;
